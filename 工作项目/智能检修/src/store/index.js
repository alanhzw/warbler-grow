import { createStore } from 'vuex'

import user from './user';
import order from './order';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user,
    order,
  }
})
